<% if(success && success.length > 0){ %>
    <div class="alert alert-success col-4 offset-4">
        <%= success %>
    </div>
    <% } %>
    
    <% if(error && error.length > 0){ %>
    <div class="alert alert-danger col-4 offset-4">
        <%= error %>
    </div>
    <% } %>