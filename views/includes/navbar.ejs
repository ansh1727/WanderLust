<nav class="navbar navbar-expand-lg sticky-top site-navbar" >
    <div class="container-fluid">
      <a class="navbar-brand brand-mark" href="/listings">
        <i class="fa-solid fa-compass"></i>
        <span>WanderLust</span>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav nav-primary">
          <a class="nav-link" href="/listings">Explore</a>
          <a class="nav-link" href="/listings/new">Host a Stay</a>
        </div>


        <div class="navbar-nav nav-search mx-auto">
          <form class="d-flex search-form" role="search" action="/listings/search" method="GET">
            <div class="search-input-shell">
              <i class="fa-solid fa-location-dot search-icon"></i>
              <input class="form-control me-2 search-inp" type="search" name="country" placeholder="Search by country, city, or title" value="<%= typeof searchQuery !== 'undefined' ? searchQuery : '' %>"/>
            </div>
            <% if (typeof searchQuery !== "undefined" && searchQuery) { %>
              <a class="btn btn-search-clear" href="/listings" aria-label="Clear search">
                <i class="fa-solid fa-xmark"></i>
              </a>
            <% } %>
            <button class="btn btn-search" type="submit"><i class="fa-solid fa-magnifying-glass"></i>Search</button>
          </form>
        </div>


          <div class="navbar-nav ms-auto nav-auth">
            <% if(!curruser) { %>
            <a class="nav-link" href="/signup"><b>Sign Up</b></a>
            <a class="nav-link nav-pill" href="/login"><b>Login</b></a>
            <% }%>
            <% if(curruser) { %>
            <a class="nav-link nav-pill" href="/logout">Log Out</a>
            <%}%>
          </div>
      </div>
    </div>
  </nav>
